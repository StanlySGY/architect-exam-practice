import fs from 'node:fs';
const mapFile=new URL('./ruankao.mm',import.meta.url);const source=fs.readFileSync(new URL('./create_freeplane_chapter10.mjs',import.meta.url),'utf8');const start=source.indexOf('const chapter10 = ');const end=source.indexOf('\n\nconst selectionResult',start);if(start<0||end<0)throw Error('Cannot locate chapter 10 data');const chapter10=Function(`${source.slice(start,end)}; return chapter10;`)();let xml=fs.readFileSync(mapFile,'utf8');if(xml.includes('TEXT="第10章 软件架构的演化和维护"'))throw Error('Chapter 10 already exists');const ids=new Set([...xml.matchAll(/\bID="(ID_\d+)"/g)].map(m=>m[1]));let nextId=1900000000;function id(){while(ids.has(`ID_${nextId}`))nextId++;const v=`ID_${nextId++}`;ids.add(v);return v;}const esc=(v,a=false)=>String(v).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll(a?'"':'\0',a?'&quot;':'\0');const detail=v=>v?`<richcontent TYPE="DETAILS" CONTENT-TYPE="plain/auto"><html><head></head><body><p>${esc(v)}</p></body></html></richcontent>`:'';const now=Date.now();let count=0;function node(x){count++;const children=(x.children||[]).map(node).join('');return `<node TEXT="${esc(x.text,true)}" ID="${id()}" CREATED="${now}" MODIFIED="${now}" AI_EDITS="true"${children?' FOLDED="false"':''}>${detail(x.details)}${children}</node>`;}const chapterXml=node(chapter10);const chapter16=xml.indexOf('<node TEXT="第16章 嵌入式系统架构设计理论与实践"');const close=xml.lastIndexOf('\n</node>\n</map>');const insertion=chapter16>=0?chapter16:close;if(insertion<0)throw Error('Cannot locate insertion point');fs.copyFileSync(mapFile,'/tmp/ruankao.mm.before-chapter10');fs.writeFileSync(mapFile,xml.slice(0,insertion)+chapterXml+'\n'+xml.slice(insertion));console.log(JSON.stringify({addedNodes:count,bytes:fs.statSync(mapFile).size}));
